# inputs_spindft_postproc.py
# Post-processing plots after DFT + TB-fit exist.

BASE_OUTDIR = "postproc_outputs_spindft"

# Only create lattice schematic for Ny <= this threshold (optional)
LATTICE_MAX_NY = 8

# "single" or "sweep_N" or "both"
PLOT_WHICH = "both"
